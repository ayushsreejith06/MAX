# MAX Shared

Shared code, types, schemas, and utilities used across frontend, backend, and contracts.

## Structure

- `types/` - TypeScript type definitions
- `schemas/` - Validation schemas
- `utils/` - Utility functions

## Usage

Import shared code in frontend and backend:

```typescript
import { User, ApiResponse } from '../shared/types';
```

